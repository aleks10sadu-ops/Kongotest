export const MAIN_MENU_PAGES = Array.from({ length: 13 }, (_, index) => `/menu-pages/main-${index + 1}.webp`);
export const BAR_MENU_PAGES = Array.from({ length: 7 }, (_, index) => `/menu-pages/bar-${index + 1}.webp`);
export const WINE_MENU_PAGES = Array.from({ length: 2 }, (_, index) => `/menu-pages/wine-${index + 1}.webp`);
